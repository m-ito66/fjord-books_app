<%= form_with(model: [@report, Comment.new]) do |form| %>
  <div class="field">
    <%= form.text_field :content %>
  </div>

  <div class="actions">
    <%= form.submit t('views.common.create_comment') %>
  </div>
<% end %>