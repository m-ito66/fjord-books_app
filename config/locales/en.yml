en:
  views:
    common:
      show: show
      edit: edit
      destroy: destroy
      new: new
      back: back
      delete_confirm: Are you sure?
      title_new: "New %{name}"
      title_edit: "Edit %{name}"
  controller:
    create:
      success: "%{name} is successfully created."
    update:
      success: "%{name} is successfully updated."
    destroy:
      success: "%{name} is successfully destroyed."
  activerecord:
    models:
      book: Book

    attributes:
      book:
        title: title
        memo: memo
        author: author
        picture: image
